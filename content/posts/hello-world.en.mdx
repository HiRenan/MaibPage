---
title: 'First commit'
description: 'Why this site exists and how it gets built, in public, piece by piece.'
date: '2026-06-21'
tags: ['meta', 'build-in-public']
---

This is the blog's first post. It exists for a simple reason: the site is the proof.
A place that talks about AI engineering has to be, itself, well engineered.

## What lives here

Technical notes on Claude Code, deterministic LLM programming, and what I learn as
I build MAIB. No fluff, just what I actually use and what breaks along the way.

- my take on AI and the dev workflow
- architecture decisions, with the reasoning behind them
- experiments, including the ones that go wrong

> What's ahead comes from executing, not decorating. Every risk I take here is on purpose.

### How the content is written

Every post is an MDX file in the repo, validated at build time. The frontmatter
runs through a schema; if something is off, the build breaks on purpose. The
indexer lives in `lib/posts.ts` and is the single source of truth:

```ts
import { getAllPosts } from '@/lib/posts';

const posts = getAllPosts('en'); // sorted by date, already validated
```

The pipeline also highlights code with a dark theme and respects the reading
measure. This site's code is open in the [repository](https://github.com/HiRenan/MaibPage),
and you can always head back [home](/).

---

Next up: the blog routes and the command palette pulling these posts in.
